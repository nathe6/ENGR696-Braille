/*
 * tables.c
 *
 *  Created on: May 6, 2025
 *      Author: Nathaniel Setiawan
 */

#include "tables.h"

void alphabetCall(int select) {
  // Pull up cell w/ alphabet[i] as the call
  // Pull up cells function as well, alphabet[i] as the call
}

void letterCall(int select) {
  // First pull up {0, 0, 1, 1, 1, 1} as the number indicator
  // if cell 3 is occupied, reset all cells and put it in cell 1
  // Pull up cells function w/ alphabet[i] as the call
}

void wordCall(int select) {
  // First select a word from the word list, save the strings' ASCII?
  // Pull up a bunch of alphabet[i] using a loop w/ length of selected string
}